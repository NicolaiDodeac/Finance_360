/** Rough planning rate for suggested tax pot (not final tax advice). */
export const SUGGESTED_TAX_POT_RATE = 0.25;

/** Business expenses at or above this amount without a receipt are flagged for review. */
export const HIGH_VALUE_EXPENSE_THRESHOLD = 100;
